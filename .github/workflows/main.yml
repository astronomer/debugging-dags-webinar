name: Astronomer CI - Deploy code

on:
  push:
    branches:
      - main
      
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Auto-pytest
      run: |
        curl -sSL install.astronomer.io | sudo bash -s
        astro dev pytest
